## Quick start

1. Cloning vimfiles:

```
    $ git clone https://github.com/lerrua/vimfiles.git ~/.vim/
 ```

2. Linking the vimrc

```
    ln -s ~/.vim/vimrc ~/.vimrc
```

3. Install bundles:

```
    :BundleInstall()
```

~ <3
